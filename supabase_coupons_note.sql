-- =========================================================
-- 折扣券備註欄位
-- =========================================================
-- 產生折扣券 / VIP 免費邀請時可填寫備註，記錄發送對象或發放原因。

ALTER TABLE public.coupons ADD COLUMN IF NOT EXISTS note text;
